{
  "name": "deadline-calendar-builder",
  "version": "1.0.0",
  "description": "Convert deadline CSVs into an ICS calendar and markdown review sheet.",
  "bin": {
    "deadline-calendar-builder": "./cli.js"
  },
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "check": "node --check cli.js && node --check lib.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
